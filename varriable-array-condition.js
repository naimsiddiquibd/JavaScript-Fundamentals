// Varriable
var bottleColor = "blue";
var waterQuantity = 1;
var isFull = false;

// Array
var items = ['bottle','mug','paper','pen'];
items.indexOf('papaer');

var items = ['bottle','mug','paper','pen'];
items.push('watch');
items.push('envelop');
items.pop();

// Conditionals
if(items.length>=4){
    console.log('you have to many stuff on your desk');
}
else{
    console.log('you have a clean desk')
}